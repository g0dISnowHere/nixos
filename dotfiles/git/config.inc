[core]
  excludesfile = ~/.config/git/ignore

# Keep larger hand-edited git preferences here instead of expanding
# Home Manager modules for every alias or workflow tweak.

#[alias]
#  st = status --short --branch
#  lg = log --graph --oneline --decorate
