# Nirinit config (dotfiles/modules/compositor/nirinit/config.toml)
# Tier 2: Persistence
# - Save interval is passed by the systemd unit via --save-interval.
# - Keep launch mappings empty until Tier 2.5 unless restore needs a known shim.

# Tier 2.5: Ignore transient windows (commented placeholder)
[skip]
apps = ["pavucontrol", "blueman-manager"]

# Tier 2.5: App-specific launch overrides (commented placeholder)
[launch]
# "Vivaldi-flatpak" = "flatpak run com.vivaldi.Vivaldi"
# "org.signal.Signal" = "flatpak run org.signal.Signal"
