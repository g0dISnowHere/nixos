set shell := ["bash", "-cu"]

sync:
    uv sync

hooks:
    pre-commit install

fmt:
    ruff check --fix .
    ruff format .

lint:
    ruff check .

typecheck:
    mypy src tests

test:
    pytest -q

test-cov:
    pytest --cov=src --cov-report=term-missing

check: fmt lint typecheck test
